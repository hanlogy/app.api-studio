import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    display: 'flex',
  },

  tabView: {},

  tabContent: {
    paddingBottom: 16,
  },

  bodyNoneText: {
    marginTop: 8,
    color: '#ccc',
    fontStyle: 'italic',
  },
});
