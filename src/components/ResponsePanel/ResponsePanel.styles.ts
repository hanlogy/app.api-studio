import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {},

  responseHeadersText: {
    fontFamily: 'Menlo',
    fontSize: 12,
    lineHeight: 20,
    color: '#666',
  },
});
