import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  name: {
    marginBottom: 10,
  },

  body: {
    borderWidth: 1,
    padding: 8,
    borderRadius: 6,
    borderColor: '#ccc',
  },

  bodyText: {
    fontFamily: 'Menlo',
    fontSize: 12,
    lineHeight: 20,
    color: '#666',
  },

  response: {},

  responseHeadersText: {
    fontFamily: 'Menlo',
    fontSize: 12,
    lineHeight: 20,
    color: '#666',
  },
});
